DATABASE_URL=postgresql://postgres:YOUR_PASSWORD_HERE@db.lcqrhqozfijmzneelaxg.supabase.co:5432/postgres

# Used to encrypt/decrypt bot_accounts passwords at rest (lib/crypto). Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Keep this secret and out of source control. Rotating it makes existing encrypted
# passwords undecryptable, so back them up (or re-enter them) before rotating.
CREDENTIALS_ENCRYPTION_KEY=
